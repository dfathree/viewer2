<%= form_with(model: thre, local: true) do |form| %>
  <% if thre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(thre.errors.count, "error") %> prohibited this thre from being saved:</h2>

      <ul>
      <% thre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :board_id %>
    <%= form.number_field :board_id %>
  </div>

  <div class="field">
    <%= form.label :num %>
    <%= form.text_field :num %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
